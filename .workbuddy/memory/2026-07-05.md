# 2026-07-05 工作记录

## Hexo 博客主题 (moeMac) 设计审查
- 对 `themes/moeMac` 主题进行了完整的设计缺陷审查（只读分析，未改代码）。
- 主题采用 macOS 桌面隐喻：首页为多个可拖拽的毛玻璃浮动窗口 + 底部 Dock 导航栏。
- 技术栈：自定义 CSS + Tailwind CDN (运行时 JIT) + DaisyUI + Font Awesome + GSAP + APlayer/Meting。
- 已识别的关键问题分类：响应式缺失、可访问性、性能、设计系统一致性、信息架构、JS bug、SEO、暗色模式缺失。
- 具体确认的 bug：gsap-animations.js 用 `getElementById('dock-bar')` 但 dock.ejs 只有 class 无 id，导致 dock 弹入动画失效；`body{user-select:none}` 全局禁选未对正文覆盖。

## moeMac 主题完善（已实施）
用户要求全面完善主题，分 8 个任务全部完成，构建通过（156 文件），本地服务 http://localhost:4000 验证 OK。
1. 修 main.js 乱码注释（10 行编码损坏的中文注释，按行号用 Python 脚本重写）。
2. 新增 `css/highlight.css`：适配 Hexo `figure.highlight` 表格结构，紫粉配色 token，含行号、复制按钮样式；head.ejs 引入。
3. 文章 TOC：post.ejs 用 Hexo `toc()` helper 生成目录，`pages.css` 加吸边面板+移动端浮层样式，main.js 加 `TOC` 对象（折叠/scroll-spy/平滑滚动/移动端 FAB）。
4. 豆瓣页：新建 `page-douban.ejs`（书/影/音三栏，10分制→5星），theme `_config.yml` 加 `douban` 配置，修占位文案。
5. 站点功能：head.ejs 加 favicon(emoji SVG 兜底)+OG/Twitter meta+theme-color；新建 `comments.ejs`(支持 waline/twikoo/giscus/gitalk，配置化，默认关)挂到文章/关于页；新建 `page-404.ejs`+`source/404.md`；搜索装 `hexo-generator-searchdb`，根 `_config.yml` 加 search 配置，新建 `search.ejs` 弹窗+`search.css`+main.js `Search` 对象（/键唤起、search.xml 索引、高亮）。
6. 阅读体验：放开正文 `user-select`；main.js 加 `CodeCopy`(复制按钮)+`LazyImg`(图片懒加载)+`BackTop`(返回顶部)；Drag 加 touchstart 触摸拖拽。
7. 清理：删 `window-music.ejs`/`window-sentence.ejs` 死代码并从 index.ejs 移除 sentence 分支；`pnpm remove hexo-theme-landscape`；分类/标签页加分页导航。
8. 美化：wall 卡片用上 `--card-bg` 渐变着色+顶部紫粉 accent 条+弹性 hover；文章表格/分割线/标题左装饰条/锚点链接样式；dock 激活指示小点。
- 注意：search.css 已在 head.ejs 引入；评论默认 enable:false，需用户填 provider 配置才生效。

## moeMac 主题完善第二轮（4 项需求）
1. 红绿灯按钮 hover：重写 `style.css` traffic lights 部分，加 macOS 风格内阴影+径向暗化+微缩放+图标渐显，win-focused 也显示。
2. 最小化 Dock 动画：修 `dock-mini-zone` 从不显示的 bug（minimize 时加 `.visible`，restore 时无项则移除）；重写 WinMgr.minimize/restore 加 Genie 吸入/弹出动画（scaleY 压缩+blur+从 dock 图标位置弹出）；dock.css 加分隔符样式+改进场动画。
3. 归档页统计美化：重写 `archive.ejs` 统计区为三宫格 hero 卡片（文章/分类/标签大号数字+图标+渐变顶条）+ 详情行（年度柱状图+热门分类标签），重写 `timeline.css` 配套样式。
4. 豆瓣接入：新建 `scripts/douban-sync.js`，注册 `hexo douban` 命令，读取 `theme.douban.user_id` 抓取豆瓣书影音收藏页（book/movie/music.douban.com），解析 HTML 提取条目，翻页+延迟，保存到 `source/_data/douban.json`；`page-douban.ejs` 优先读 `site.data.douban` 回退到 `theme.douban`；配置加 `user_id`/`cookie` 字段。
- 构建 156 文件通过，本地服务 http://localhost:4000 验证 OK。

## moeMac 主题完善第三轮（3 项修复）
1. 红绿灯 hover 不生效：根因是效果太微弱（13px 按钮 scale 1.12 几乎看不出）。重写为 14px 按钮 + scale(1.25) + 各按钮独立 hover 背景色加深 + 彩色发光环 + 图标全显 + 按下 brightness(0.78) 按压感。
2. 最小化收不进 Dock：根因是 `dock-mini-zone` 是 `.dock-bar`（全屏宽 fixed）的子元素，`left:100%` 推到屏幕外。修复：dock.ejs 把 `dock-mini-zone` 移到 `.dock-bar-inner` 内部；dock.css 改为 flex 子元素 + `max-width` 过渡（0→500px）+ opacity 渐显，不再用 absolute 定位。
3. 滚动条问题：`html` 从 `overflow-y:scroll` 改为 `auto`（不再强制显示）；新增 `body.is-desktop` 类（首页 `overflow:hidden` 禁止滚动）；main.js 加 `DesktopMode.check()` 检测 `.drag-win` 自动切换；全局滚动条美化（8px 宽 + 紫粉渐变 thumb + hover 加深 + Firefox `scrollbar-color` 支持）。
- 构建 156 文件通过，本地服务 http://localhost:4000 验证 OK。

## moeMac 主题完善第五轮（豆瓣图片防盗链修复）
- 问题：用户反馈书籍图片在博客上显示不出来。
- 根因：**豆瓣图片服务器有防盗链机制**——非 `Referer: douban.com` 的请求返回 HTTP 418，博客页面在浏览器中加载时 Referer 是博客域名，被豆瓣拒绝。
- 修复方案：`douban-sync.js` 新增 `downloadCover()` 函数，抓取时带 Referer 头下载封面图到 `source/images/douban/{book,movie}/` 目录；JSON 数据新增 `cover_local` 字段保存本地路径；`page-douban.ejs` 优先用 `cover_local` 引用本地图片；图片加载失败时 onerror 替换为占位图标。
- extractItem 增强：支持 `data-src` 懒加载图片、`//` 协议相对 URL、提取 subject ID 和条目链接。
- 重新抓取 6 本书 + 10 部电影，16 张封面图全部下载成功，构建 172 文件通过。
- 本地服务 http://localhost:4000 验证：图片 HTTP 200 OK，Content-Type: image/jpeg。

## moeMac 主题完善第六轮（豆瓣名称修复）
- 问题：Avengers 系列电影抓到的是英文名，豆瓣页面里有中文名。
- 根因：电影条目 HTML 有两处标题——图片 `<a title="Avengers: Endgame">` 是英文名，`<em>` 标签里是完整中英文名 `复仇者联盟4：终局之战 / Avengers: Endgame`。旧代码优先取 `title` 属性，拿到的是英文名。
- 修复：`extractItem()` 改为优先从 `<em>` 标签提取，按 `/` 分隔取第一部分作中文名（`name`），其余部分存为 `original_name`。无 `<em>` 时回退到 `title` 属性（书籍）。
- `page-douban.ejs` 在标题下加 `<p class="douban-original-name">` 显示英文原名；`pages.css` 加副标题样式（12px 灰色）。
- 重新抓取后 4 部 Avengers 电影正确显示中文名 + 英文副标题。

## moeMac 主题完善第七轮（红绿灯 hover + Dock 胶囊彻底重写）
1. 红绿灯 hover 不生效/太丑：
   - 根因：按钮只有 14px，scale(1.25) 后 17.5px 变化不明显；Font Awesome `<i>` 图标渲染受字体加载影响，hover 时 opacity 变化看不清。
   - 修复：按钮加大到 16px；图标从 Font Awesome `<i>` 改为内联 SVG（精确的 X/减号/图钉形状，9px viewBox）；hover 效果改为 scale(1.3)+brightness(0.82)+背景色加深，更接近 macOS 原生；去掉复杂的 box-shadow（之前多层阴影反而让按钮看起来糊）。
2. 最小化窗口不进 Dock 胶囊：
   - 根因：`dock-mini-zone` 用 `position:absolute;left:100%` 定位到 dock-bar-inner 外部，图标出现在玻璃胶囊外面。
   - 修复：改回 flex 子元素 + `max-width:0→600px` + `overflow:hidden` 过渡，图标在 dock-bar-inner 内部，视觉上在胶囊里。
   - 抖动根因消除：`.dock-item:hover .dock-item-inner` 去掉 `margin:0 4px`（之前每次 hover 都改变 flex 布局导致 dock 重排），改为纯 `transform`。
- 构建 172 文件通过，本地服务 http://localhost:4000 验证 OK。

## moeMac 主题完善第八轮（红绿灯 tooltip 美化）
- 问题：红绿灯按钮 hover 时弹出浏览器原生 `title` tooltip，样式丑陋。
- 修复：`title` → `data-tip`，CSS 用 `::after` + `::before` 伪元素做 macOS 风格毛玻璃 tooltip（rgba(0,0,0,0.72) + backdrop-filter:blur(12px)，圆角 6px，小三角箭头，150ms 延迟淡入）；按钮 hover 的 scale 效果从按钮本身移到内层 SVG（`svg:hover{transform:scale(1.2)}`），避免 tooltip 被缩放；按钮 hover 改用 box-shadow 环 + brightness 做视觉反馈。
- 构建 172 文件通过，本地服务 http://localhost:4000 验证 OK。

## moeMac 主题完善第十轮（用 GSAP 统一重写所有动画）
- 用户指出主题已引入 GSAP + ScrollTrigger + gsap-animations.js，但之前一直手写 CSS transition / Web Animations API，没有利用现有库。
- 改动：
  1. 红绿灯 hover：CSS `transform:scale` → GSAP `gsap.to(btn,{scale:1.35,ease:'back.out(2.5)'})`，带弹性回弹；SVG 图标用 GSAP 淡入；tooltip 用 CSS 变量 `--tip-show` + GSAP `delayedCall(0.3)` 延迟控制
  2. Dock 最小化/恢复：Web Animations API → GSAP `gsap.timeline()` 串联，窗口吸入(0.42s) → 图标弹入(0.32s) 用 `'-=0.1'` 微重叠；恢复时窗口展开+图标缩小同步进行
  3. Dock hover：CSS transition → GSAP `back.out(2.5)` 弹性放大，比 CSS `cubic-bezier` 更有弹性手感
  4. ScrollTrigger 滚动触发：文章列表卡片批量入场(`ScrollTrigger.batch`)、归档时间线逐项淡入、文章内图片滚动到视口淡入
  5. 友链卡片 hover：GSAP `back.out(2)` 弹性放大
  6. 降级保护：minimize/restore 检查 `typeof gsap !== 'undefined'`，GSAP 不可用时降级为直接显示/隐藏
  7. CSS 移除所有 `transition:transform` 和 `:hover{transform:...}`，避免与 GSAP 冲突
- gsap-animations.js 新增方法：dockItems()、articleImages()；run() 加入 ScrollTrigger.refresh()
- 构建 172 文件通过，本地服务 http://localhost:4000 验证 OK。

## moeMac 主题完善第十一轮（修复第十轮引入的两个 bug）
- Bug 1：红绿灯 SVG 图标不显示。根因：CSS `svg{opacity:0}` 且 GSAP `trafficButtons` 的 `onmouseenter` 可能因时序问题未生效；tooltip 用的 `--tip-show` CSS 变量方案不可靠。修复：SVG `stroke`/`fill` 改为 `#fff`（白色在彩色按钮上可见）；CSS 加兜底 `.app-window:hover .win-traffic-btn:hover svg{opacity:1;}`；tooltip 改回纯 CSS `:hover` 控制（`::after`/`::before` opacity 0.15s 延迟过渡）。
- Bug 2：最小化后左上角出现窗口。根因：`minimize` 的 `onComplete` 里 `clearProps` 在 `display:none` 之前执行，清掉了 `display:none`；且 GSAP `x/y` transform 与 Drag 的 `left/top` 冲突导致位置丢失。修复：`onComplete` 里先 `clearProps:all` 再设 `display:none`（顺序关键）；保存 `savedLeft/savedTop`（Drag 定位值），`clearProps` 后恢复，窗口位置不丢失。
- 去掉 `.win-traffic-btn` 上残留的 `transition:transform 0.18s`（与 GSAP 冲突）。
- 构建 172 文件通过。
1. 红绿灯 hover 变灰：根因是 `filter:brightness(0.82)` + 各按钮 `:hover{background:更深色}`。修复：去掉所有 `filter:brightness`、去掉 `:hover{background:...}` 规则，hover 只保留 `transform:scale(1.3)`，颜色不变。
2. 最小化图标 hover 被裁切：根因是 `dock-mini-zone` 用 `overflow:hidden` 裁剪了 hover 放大区域。修复：改 `overflow:visible`，显隐从 `max-width` 过渡改为 `opacity/visibility`（零布局回流）。
3. 最小化图标 hover 效果与普通 dock-item 不一致：从 `translateY(-8px) scale(1.25)` 改为 `translateY(-10px) scale(1.3)`，完全一致。
4. 动画不丝滑：根因是 dock 图标 CSS 动画与窗口 Genie 动画同时播放不同步。修复：dock 图标改为 Web Animations API，在窗口 Genie 动画 `onfinish` 后才弹入（420ms 窗口吸入 → 320ms 图标弹入，串行不重叠）；restore 同理，dock 图标飞出(280ms) 与窗口展开(380ms) 同步进行。
- 构建 172 文件通过，本地服务 http://localhost:4000 验证 OK。
2. 滚动条抖动：`html` 加 `scrollbar-gutter:stable` 预留滚动条空间；`html.is-desktop` 也设 `overflow:hidden`；`DesktopMode.check()` 同时在 `documentElement` 和 `body` 上切换 `is-desktop` 类。
3. 最小化图标 hover 抖动：`.dock-minimized-item` 不再继承 `.dock-item` 类；独立定义 hover 样式（纯 transform，显式 `margin:0` 覆盖 `.dock-item:hover` 的 `margin:0 4px`），零布局回流。
4. 动画流畅度：用 Web Animations API (`element.animate()`) 重写 minimize/restore，动画结束后 `anim.cancel()` 清除 fill 状态，无残留 inline style；双 rAF 确保 dock 图标渲染后再算位置。
5. 豆瓣抓取：脚本 `douban-sync.js` 修复两个问题——(a) `this.theme.config` 在 console 命令中为空，需先 `await this.load()`；(b) 豆瓣改版后书籍用 `<li class="subject-item">`、电影用 `<div class="item comment-item">`，重写 `parseItems()` 用 split 方式提取两种结构；修复封面 URL 升级正则（`/view/subject/s/` → `/view/subject/l/`）。已成功抓取 6 本书 + 10 部电影。
- 构建 156 文件通过，本地服务 http://localhost:4000 验证 OK。
## moeMac 主题完善第十二轮（Dock 宽度动画平滑化）
- 问题：用户反馈"收入dock栏胶囊后 dock栏加宽 恢复时缩小宽度的动画 显得生硬不流畅"。
- 根因：`dock-mini-zone` 用 CSS `opacity/visibility` 显隐，导致 dock-bar（flex布局）内容宽度突变，没有平滑过渡动画。
- 修复方案：
  1. `dock.css`：`.dock-mini-zone` 改为 `width:0; overflow:hidden; flex-shrink:0;`，用 GSAP 动画 width 展开/收起；去掉 `.visible` 类，改用 `.expanded` 类标识状态。
  2. `main.js` `WinMgr.minimize()`：添加 item 后，先设 `zone.style.width = '0px'`，获取 `targetWidth = zone.scrollWidth`，然后用 GSAP timeline 并行动画窗口吸入 + dock 图标弹入 + zone width 从 0 到 targetWidth。
  3. `main.js` `WinMgr.restore()`：判断 zone 是否只剩当前 item（`shouldCollapseZone`），如果是则并行动画 zone width 从 currentWidth 到 0；如果还有其他 item，则在窗口展开完成后移除当前 item 并 GSAP 动画 zone width 到新 scrollWidth。
  4. timeline onComplete 中恢复 `zone.style.width = 'auto'`（展开时）或设为 `'0px'`（收起时）。
- 构建 172 文件通过，本地服务 http://localhost:4000 验证 OK。

## moeMac 主题完善第十三轮（修复最小化/恢复的两个 bug）
- Bug 1：恢复时窗口跑到左上角。
  根因：`minimize()` 里变量名叫 `zone`，但 GSAP timeline 里误写为 `zone`（3 处），导致 dock 宽度动画 JS 报错、`savedLeft`/`savedTop` 未被正确保存到 `dataset`。
  修复：`zone` → `zone` 统一变量名；`minimize()` 在 `onComplete` 前把位置写入 `win.dataset.savedLeft`/`savedTop`；`restore()` 开头读取 dataset，timeline 末尾 `clearProps` 后再显式写回 `win.style.left`/`top`。
- Bug 2：红绿灯 SVG 图标不显示。
  根因：CSS 只在 `.win-traffic-btn:hover svg` 时才 `opacity:1`，但按钮仅 16px，鼠标很难精准 hover 到按钮（而不是窗口），导致图标几乎不显示。
  修复：CSS 分层控制——窗口 hover 时所有 SVG `opacity:0.45`（半显诱导），按钮 hover 时该按钮 SVG `opacity:1`（全显）；SVG 加大到 10px、`stroke-width:1.6` 更粗更明显。
- 构建 172 文件通过。

## moeMac 主题完善第十四轮（修复红绿灯三个 bug）
- Bug 1：图标时显时不显。根因：GSAP `trafficButtons()` 在 `mouseleave` 时 `gsap.to(svg,{opacity:0})` 写了 inline style，永久覆盖 CSS `:hover` 规则（inline 优先级 > CSS class）。修复：从 GSAP 中删除所有 SVG opacity 操作，SVG 透明度完全交给 CSS `:hover` 分层控制。
- Bug 2：置顶标消失。根因：CSS `.win-traffic-btn svg{fill:none}` 覆盖了 pin 按钮的 `fill="#fff"`（SVG 内联属性优先级低于 CSS）。修复：从 CSS 通用规则中删除 `fill:none;stroke:#fff;stroke-width:1.6`，让各 SVG 的内联属性各自生效（close/minimize 用 stroke，pin 用 fill）；新增 `.win-btn-pin.is-pinned svg{opacity:1}` 让置顶状态常显。
- Bug 3：恢复时三圈不该显示。根因：restore 动画期间窗口从 0.1 缩放放大，红绿灯跟着显示。修复：`restore()` 开头加 `win.classList.add('win-restoring')`，CSS 加 `.app-window.win-restoring .win-traffic{opacity:0!important}`；timeline onComplete 时移除该类。
- 构建 172 文件通过。

## moeMac 主题完善第十五轮（ui-layouts 风格增强 + Dock hover bug 修复）
### 1. Dock hover bug 修复
- 问题：最小化窗口图标 hover 影响其他导航，触摸设备 hover 状态 sticky。
- 根因：
  (a) `.dock-mini-zone` 的 `overflow:hidden` 在展开后仍保持，裁切了 hover 放大区域；
  (b) GSAP `dockItems()` 只处理 `.dock-item:not(.dock-minimized-item)`，最小化图标没有 GSAP hover 动画；
  (c) 触摸设备 `:hover` 会 sticky。
- 修复：
  (a) `.dock-mini-zone` 动态切换 overflow——动画期间 `hidden`，动画完成后加 `.expanded` 类切换到 `visible`；
  (b) GSAP `dockItems()` 统一处理所有 `.dock-item-inner`（包括 `.dock-minimized-item`）；
  (c) CSS 加 `@media (hover:none)` 清除触摸设备的 sticky hover；
  (d) `.dock-minimized-item` 加 `isolation:isolate` 防止 z-index 影响相邻元素。

### 2. UI-Layouts 风格增强（新建 `ui-enhance.css` + `ui-enhance.js`）
受 ui-layouts.com 启发，用 GSAP + Canvas + CSS 实现 10 项增强效果：
1. **动态网格背景** (Mesh Gradient)：4 个渐变色块缓慢漂浮+缩放，替代静态 radial-gradient
2. **光标粒子拖尾** (Sparkles)：Canvas 实现鼠标移动时生成彩色粒子，带重力+衰减+光晕
3. **3D 卡片倾斜** (Tilt Cards)：wall-card/friend-card/post-list-item/douban-card 鼠标跟随 perspective tilt + 光泽层
4. **数字滚动动画** (Motion Numbers)：`data-motion-num` 属性的元素滚动到视口时从 0 滚到目标值
5. **点击涟漪** (Ripple Effect)：dock-item/win-traffic-btn/page-btn 等点击时产生水波纹
6. **Clip-Path 图片揭示** (Clip Reveal)：文章/墙/豆瓣卡片图片从左到右 clip-path 展开
7. **Dock 放大镜** (Dock Magnify)：鼠标附近的 dock 图标逐渐放大（macOS 原生放大镜效果）
8. **磁吸按钮** (Magnetic Buttons)：红绿灯/返回顶部/分页按钮鼠标靠近时被吸引偏移
9. **窗口玻璃扫光** (Glass Shine)：窗口 hover 时一道光泽从左到右扫过
10. **Hero 头像浮动 + 标题渐变闪烁** (Float + Shimmer)
- `prefers-reduced-motion` 媒体查询尊重用户减少动画偏好
- `initOnce()` 只在页面加载时调用一次（sparkles canvas），`init()` 每次 AJAX 导航后重新调用
- head.ejs 引入 `ui-enhance.css`，scripts.ejs 引入 `ui-enhance.js`
- main.js DOMContentLoaded 和 AJAX Nav.load 中调用 `UIEnhance.init()`
- gsap-animations.js run() 中集成 UIEnhance 增强方法
- 构建 174 文件通过，本地服务 http://localhost:4000 验证 OK。

## moeMac 主题完善第十六轮（红绿灯 hover 复原 + restore 不显示圆圈）
- Bug 1：鼠标 hover 放大后移开不复原。
  根因：GSAP `trafficButtons()` 用 `onmouseenter/onmouseleave` 写 inline `transform:scale(1.35)`。点击最小化按钮时窗口 `display:none`，`onmouseleave` 不触发，inline transform 残留。`killAll()` 也未清理 `.win-traffic-btn` 的 transform。
  修复：
  (a) **彻底移除 GSAP hover**，改用纯 CSS `:hover{transform:scale(1.3)}` + `:active{transform:scale(1.15)}`，`transition:transform 0.18s cubic-bezier(0.34,1.56,0.64,1)` 模拟 back.out 弹性。CSS 在元素隐藏/鼠标离开时自动重置，无 inline 残留。
  (b) `@media (hover:hover)` 包裹，触摸设备不触发 sticky hover。
  (c) `killAll()` 的 clearProps 选择器加入 `.win-traffic-btn`，清理旧 inline transform。
  (d) `trafficButtons()` 改为仅清除残留 inline transform，不再绑定 GSAP 事件。
- Bug 2：restore 后左上角圆圈不该显示。
  根因：`restore()` 第 305 行 `Drag.focus(win)` 同步执行，立即加 `win-focused` 类。CSS `.app-window.win-focused .win-traffic{opacity:1}` 导致动画结束后（`win-restoring` 移除后）红绿灯立即弹出。
  修复：
  (a) CSS 移除 `.app-window.win-focused .win-traffic` 规则——红绿灯仅在 `:hover` 时显示，不再因 focus 常显。
  (b) `Drag.focus(win)` 从同步调用改为移入 timeline `tl.call` 回调，在 `win-restoring` 移除之后执行。
  (c) GSAP 降级路径也补充 `win.classList.remove('win-restoring')`。
- 构建 174 文件通过。

## moeMac 主题完善第十七轮（Dock hover 复原 + 返回顶部对齐）
### 1. Dock hover 放大不还原
- 根因：两套 GSAP hover 系统冲突——`GSAPAnimations.dockItems()` 用 `onmouseenter/onmouseleave` 写 inline `transform:scale(1.3)`，`UIEnhance.dockMagnify()` 用 `mousemove` 也写 inline transform 到同一个 `.dock-item-inner`。AJAX 导航时 `onmouseleave` 不触发，inline transform 残留。
- 修复：
  (a) 移除 GSAP `dockItems()` 的 hover 逻辑，改用纯 CSS `:hover{transform:translateY(-8px) scale(1.25)}` + `transition:cubic-bezier(0.34,1.56,0.64,1)` 模拟弹性。
  (b) `dockMagnify()` 保留（放大镜效果），但补加 `mouseleave` 重置 `scale:1,y:0`。
  (c) `killAll()` clearProps 选择器加入 `.dock-item-inner`。
  (d) `@media(hover:hover)` 包裹，触摸设备不触发。

### 2. 放大效果被胶囊遮挡
- 根因：`.dock-bar-inner` 的 `backdrop-filter` + `border-radius` 在某些浏览器创建裁切上下文。
- 修复：`.dock-bar-inner` 加 `overflow:visible`。

### 3. 去掉底下的点
- 移除 `.dock-item.active::after`（激活指示小点）及 `@keyframes dock-dot-in`。

### 4. 导航文字 hover 显示
- `.dock-text` 从 `top:-40px` 改为 `bottom:calc(100% + 8px)`（更可靠地定位在 item 上方），`z-index:20`。
- `@media(hover:hover)` 包裹 hover 显示规则。

### 5. 返回顶部按钮对齐 + hover 抖动
- 根因：`bottom:150px` 太高；CSS `:hover{transform:translateY(-3px)}` + `transition:all` 与 GSAP `magneticButtons()` 的 inline `transform:translate(x,y)` 冲突，双重 transform 动画导致抖动。
- 修复：
  (a) `bottom` 从 `150px` 改为 `14px`（与 dock 栏同一水平线）。
  (b) 从 `magneticButtons()` 选择器中移除 `.back-top-btn`。
  (c) CSS `transition:all` 改为 `transition:transform 0.25s cubic-bezier(...),box-shadow 0.25s,color 0.25s`。
  (d) hover transform 改为 `translateY(-4px)` + `cubic-bezier(0.34,1.56,0.64,1)` 弹性曲线。
- 构建 174 文件通过。

## moeMac 主题完善第十八轮（Dock 普通导航 hover 文字不显示）
- Bug：普通 dock 导航 hover 不显示文字标签，但最小化窗口图标可以。
- 根因：`ui-enhance.css` 中 `.dock-bar-inner{perspective:600px}` 创建 3D 渲染上下文。`.dock-item-inner` hover 时有 `transform`，在 perspective 下 3D 渲染导致 `.dock-text`（同级元素）被渲染到变换元素之后。`.dock-minimized-item` 有 `isolation:isolate` 形成独立层叠上下文，所以不受影响。
- 修复：`.dock-item` 也加 `isolation:isolate`，与 `.dock-minimized-item` 一致，创建独立层叠上下文，隔离 perspective 的 3D 渲染干扰。
- 构建 174 文件通过。

## moeMac 主题完善第十九轮（Dock tooltip 修复 + 文章卡片标签）
### 1. Dock 普通导航 hover 文字不显示（第二轮修复）
- 上一轮加了 `isolation:isolate` 但没有彻底解决。
- 根因：`ui-enhance.css` 中 `.dock-bar-inner{perspective:600px}` 创建 3D 渲染上下文。`.dock-item-inner` hover 时 `transform:translateY(-8px) scale(1.25)` 在 perspective 下做 3D 渲染，导致同级 `.dock-text`（无 Z 轴变换）被渲染到变换元素之后。`isolation:isolate` 只创建层叠上下文，不阻止父元素的 perspective 对子元素生效。
- 修复：直接移除 `.dock-bar-inner{perspective:600px}`。dock 放大镜效果用 `scale+y` 不需要 perspective；3D 倾斜效果（tiltCards）在各自卡片元素上设置 perspective，不依赖 dock 的。
### 2. 文章列表卡片不显示标签
- 根因：`post-list.ejs` 模板只渲染了 title/date/categories/excerpt，没有渲染 tags。
- 修复：
  (a) `post-list.ejs` 在 excerpt 下方加 `<% if (post.tags && post.tags.length > 0) %>` 遍历输出 `.post-list-tag` 链接。
  (b) `postlist.css` 加 `.post-list-tag` 样式：紫色小胶囊 `background:rgba(167,139,250,0.1); color:#7c3aed`，hover 加深 + `translateY(-1px)`。
- 构建 174 文件通过。

## moeMac 主题完善第二十轮（Dock tooltip 彻底修复 + 文章卡片标签修复）
### 1. Dock tooltip 不显示（第三轮修复，彻底解决）
- 之前两轮尝试：(1) 加 `isolation:isolate` (2) 移除 `perspective:600px`，均未解决。
- 真正根因：`.dock-bar-inner` 上的 `backdrop-filter:blur(50px)` 创建了隐式裁切上下文。即使设了 `overflow:visible`，Chrome/WebKit 仍会将内容裁切到元素的 border-box 范围内。`.dock-text` 定位在 `bottom:calc(100% + 8px)`（即 `.dock-item` 上方），超出了 `.dock-bar-inner` 的顶部边界，因此被裁切不可见。
- 修复：将 `backdrop-filter` 及相关视觉效果（background/border/box-shadow/border-radius）从 `.dock-bar-inner` 本身移到 `::before` 伪元素上。`::before` 用 `position:absolute; inset:0; z-index:-1` 覆盖在 dock 区域，提供玻璃效果。`.dock-bar-inner` 本身设为 `background:transparent; border:none; overflow:visible`，不再有 `backdrop-filter`，因此不会创建隐式裁切，`.dock-text` 可以自由溢出显示。
- 同时移除了 `.dock-item` 上的 `isolation:isolate`（不再需要）。

### 2. 文章页面卡片标签不显示
- 根因：文章页面用的是 `posts-wall.ejs`（墙布局），不是 `post-list.ejs`（列表布局）。`posts-wall.ejs` 第 27 行 `var showTags = size !== 'card-sm'` 导致小尺寸卡片不显示标签。而 `post-list.ejs` 上一轮加的标签根本没被使用。
- 修复：`posts-wall.ejs` 中 `var showTags = true`，所有卡片都显示标签。
- 验证：生成 HTML 中 `card-tags` 出现 24 次（25 篇文章中 1 篇 hello-world.md 无标签）。
- 构建 174 文件通过。

## moeMac 主题完善第二十一轮（Dock tooltip 彻底修复 — 独立玻璃层）
- 前三轮修复均未解决：isolation:isolate、移除 perspective、移除 backdrop-filter 到 ::before。
- 真正根因：无论 backdrop-filter 放在 .dock-bar-inner 本身还是 ::before 伪元素上，
  Chrome/WebKit 都会将 .dock-bar-inner 提升为合成层（compositing layer），
  导致 overflow:visible 不按预期工作，.dock-text（定位在 dock 上方）被裁切。
  .dock-minimized-item 在 .dock-mini-zone 内，展开后 .dock-mini-zone 有 overflow:visible
  且自身有 isolation:isolate 形成独立层叠上下文，不受 .dock-bar-inner 合成层影响。
- 最终修复方案：将玻璃效果完全从 .dock-bar-inner 及其伪元素中剥离，
  创建独立的 .dock-glass div（与 .dock-bar-inner 平级），
  backdrop-filter / background / border / box-shadow 全放在 .dock-glass 上。
  .dock-bar-inner 设为完全透明（无 backdrop-filter / overflow / border-radius / border），
  仅有 position:relative; z-index:1。
  .dock-glass 用 position:absolute; z-index:0 定位在 dock-bar 内。
  宽度由 JS syncDockGlass() 函数动态同步到 .dock-bar-inner 的 offsetWidth。
  在 DOMContentLoaded、dockHL、minimize onComplete、restore onComplete 中调用 syncDockGlass()。
- dock.ejs 模板在 .dock-bar-inner 前加 <div class="dock-glass"></div>。
- 构建 174 文件通过。

## moeMac 主题完善第二十二轮（Dock tooltip — JS 方案彻底解决）
- 前四轮 CSS 方案全部失败（isolation、移除 perspective、移除 backdrop-filter、独立 .dock-glass div）。
- 诊断测试发现：CSS :hover 在 position:fixed 的 dock 容器内不可靠——
  测试 A（简单 relative 元素）:hover 正常；
  测试 B（position:fixed dock 结构 + CSS :hover）不显示；
  测试 C（相同结构 + JS mouseenter/mouseleave）正常显示。
- 最终方案：放弃 CSS :hover，改用 JS 事件控制 tooltip：
  (a) CSS：`.dock-text` 默认 `opacity:0`，`.dock-text.tip-show{opacity:1}`。
      移除所有 `@media (hover:hover)` 和 `@media (hover:none)` 的 tooltip 规则。
      移除 `.dock-text` 上的 `backdrop-filter`（避免合成层干扰）。
  (b) `gsap-animations.js` `dockItems()`：遍历 `.dock-item, .dock-minimized-item`，
      给 `.dock-text` 子元素绑定 `mouseenter` → `classList.add('tip-show')`，
      `mouseleave` → `classList.remove('tip-show')`。用 `dataset.tipInit` 防重复绑定。
  (c) `main.js` `WinMgr.minimize()`：动态创建的 `.dock-minimized-item` 也绑定同样的 tooltip 事件。
- 构建 174 文件通过。

## moeMac 主题完善第二十三轮（Dock tooltip — 内联事件彻底解决）
- 前五轮修复全部失败：isolation、移除 perspective、移除 backdrop-filter、独立 .dock-glass div、JS addEventListener。
- 诊断测试确认：CSS :hover 和 JS addEventListener 在简单测试页都能工作，但在实际主题页面中不工作。
  可能原因：main.js IIFE 中有运行时错误导致 DockTip 未定义或 init() 未执行；或外部 JS 加载顺序问题；
  或 gsap-animations.js 中 killAll() 的 gsap.set 影响了 DOM。无法确定具体原因。
- 最终方案：在 dock.ejs 模板中直接用内联 onmouseenter/onmouseleave 事件绑定 tooltip。
  不依赖任何外部 JS 文件（main.js / gsap-animations.js / ui-enhance.js）。
  - onmouseenter: var t=this.querySelector('.dock-text');if(t)t.classList.add('tip-show')
  - onmouseleave: var t=this.querySelector('.dock-text');if(t)t.classList.remove('tip-show')
  - CSS: .dock-text{opacity:0} .dock-text.tip-show{opacity:1}
  - 动态创建的 .dock-minimized-item 仍由 main.js DockTip.init() 绑定
- 构建 174 文件通过，生成的 HTML 确认内联事件存在。

## moeMac 主题完善第二十四轮（Dock tooltip 根因找到 — ripple-target overflow:hidden）
- 前六轮修复全部失败：isolation、移除 perspective、移除 backdrop-filter、独立 .dock-glass div、JS addEventListener、内联事件。
- 诊断测试确认：即使 opacity:1 !important，tooltip 仍然不可见 → 不是事件问题，是 CSS 渲染问题。
- 根因：`ui-enhance.js` 的 `rippleEffect()` 给 `.dock-item` 和 `.dock-minimized-item` 添加了 `.ripple-target` 类。
  CSS `.ripple-target{position:relative;overflow:hidden;}` 的 `overflow:hidden` 裁切了 `.dock-text`（定位在 `.dock-item` 上方，超出其边界）。
  最小化窗口图标之所以正常，是因为 `.dock-minimized-item` 有 `isolation:isolate` 创建独立层叠上下文……不对，`overflow:hidden` 也会裁切。
  实际上最小化图标可能是在 `ripple-target` 添加之前就已经显示了 tooltip（通过 CSS :hover），
  或者 `.dock-mini-zone.expanded` 的 `overflow:visible` 覆盖了 `.ripple-target` 的 `overflow:hidden`。
- 修复：从 `rippleEffect()` 的选择器中移除 `.dock-item` 和 `.dock-minimized-item`，不再给它们加 `.ripple-target` 类。
- 还原 dock.ejs 为无内联事件，还原 CSS 为 `:hover` 控制 opacity（不再需要 tip-show 类）。
- 构建 174 文件通过。
